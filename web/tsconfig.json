{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "//": "worker/, db/ and the D1 example are Cloudflare Workers runtime glue from the vinext starter; their ambient types (Fetcher, D1Database, cloudflare:workers) come from the wrangler/vinext build, not the app's DOM lib, so they are type-checked there rather than in the app tsc pass.",
  "exclude": ["node_modules", "worker", "db", "drizzle.config.ts", "examples"]
}
